import React, {Component} from 'react';

export class onStatusTires extends Component {
    render = (props) => {

let tiresElements = this.props.tires.map(t => < )
        return (

        );
    }
}